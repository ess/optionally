require 'spec_helper'

describe Optionally do
  it 'has a version number' do
    expect(Optionally::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
